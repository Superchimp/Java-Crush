package day1.exercises;
import java.util.*;

public class A {
	
	static boolean exists(int[] ints, int k) {
		
		int length = ints.length;
		int shortcut = 0;
		int counter = length;
		
		
		if (length > 1000) {
			while ( k > ints[shortcut]) {
				shortcut = counter / 2;
			
				if (k > ints[shortcut]) {
					if ((length - ) )
			}
			}
		
		}

}
